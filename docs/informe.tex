% ============================================================================
%  Informe tecnico - Proyecto 2: Sistema Multimodal de Recuperacion y Busqueda
%  CS2042 Bases de Datos II - UTEC - Ciclo 2026-1
%
%  Formato: IEEE conference (IEEEtran).
%  Compilar: pdflatex informe.tex  (dos veces, para refs y numeracion)
%  Las figuras salen de ../backend/experiments/results/ (los benchmarks las
%  generan). Si compilas en Overleaf, sube los PNG y ajusta \graphicspath.
% ============================================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
% es-noshorthands: evita que babel se pelee con los simbolos de los listados
\usepackage[spanish,es-noshorthands]{babel}
\usepackage{url}
\usepackage{booktabs}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}

\graphicspath{{../backend/experiments/results/}}

\definecolor{codegray}{gray}{0.95}
\lstset{
  basicstyle=\ttfamily\scriptsize,
  backgroundcolor=\color{codegray},
  breaklines=true,
  frame=single,
  framesep=3pt,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
}

\begin{document}

% ══════════════════════════════════════════════════════════════
\title{Sistema Multimodal de Recuperación y Búsqueda:\\
Índice Invertido con Codebook frente a las Técnicas Nativas de PostgreSQL}

% Si hay mas integrantes, agregarlos aqui separados por coma
\author{
\IEEEauthorblockN{Jyns Arturo Ordóñez Del Carpio, Denzel Bautista, Gian Aedo}
\IEEEauthorblockA{Base de Datos II (CS2042) --- UTEC --- Lima, Perú}
}

\maketitle

% ══════════════════════════════════════════════════════════════
\begin{abstract}
Presentamos un motor de recuperación por similitud que aplica una única
arquitectura --- \textit{split} $\rightarrow$ extractor $\rightarrow$
\textit{codebook} $\rightarrow$ índice invertido $\rightarrow$ búsqueda por
coseno --- a tres modalidades de contenido: texto (18\,454 letras de
canciones), imagen (44\,326 productos de moda) y audio (1\,000 pistas de
GTZAN). Cada modalidad solo cambia el extractor de características (tokens
lingüísticos, SIFT o MFCC) y la construcción del diccionario (top-$k$ de
palabras o K-Means, siguiendo el esquema \textit{Bag of Visual Words}); el
índice invertido se construye con SPIMI y los histogramas se ponderan con
TF-IDF sublineal. Sobre el motor se implementan tres aplicaciones (búsqueda
visual e-commerce, búsqueda musical por letra y por similitud acústica, y una
consola de consultas multimodal) y se compara contra las técnicas nativas de
PostgreSQL: \texttt{GIN} full-text para texto y \texttt{pgvector} con índice
HNSW para vectores. Los experimentos muestran que el índice propio y
\texttt{pgvector} recuperan exactamente los mismos resultados
(recall $=1.0$), que el índice propio domina la latencia hasta corpus
medianos, y que HNSW acelera la búsqueda vectorial 12--32$\times$ con recall
de hasta $0.96$.
\end{abstract}

\begin{IEEEkeywords}
Índice invertido, SPIMI, TF-IDF, Bag of Visual Words, SIFT, MFCC, K-Means,
similitud coseno, PostgreSQL, GIN, pgvector, HNSW, búsqueda aproximada.
\end{IEEEkeywords}

% ══════════════════════════════════════════════════════════════
\section{Introducción}

Las bases de datos modernas deben indexar contenido no estructurado ---
texto, imágenes, audio --- y responder consultas por \emph{similitud}, no por
igualdad exacta. Este proyecto demuestra que los tres tipos de contenido
pueden indexarse y recuperarse con \emph{el mismo paradigma}: dividir el
contenido en unidades atómicas (\textit{chunks}), extraer descriptores
propios de cada modalidad, cuantizarlos contra un diccionario compartido
(\textit{codebook}) y publicar el resultado en un índice invertido consultado
por similitud coseno.

Los objetivos son: (1) implementar la arquitectura unificada de punta a punta;
(2) persistirla en PostgreSQL; (3) compararla contra los índices nativos del
motor (GIN/GiST para texto, \texttt{pgvector} para vectores); y (4) evaluar
experimentalmente latencia, throughput, \textit{recall} y memoria para
cuantificar los \textit{trade-offs}.

\subsection{Aplicaciones implementadas}
El backend soporta tres aplicaciones (el enunciado exige al menos dos):
\begin{enumerate}
  \item \textbf{Búsqueda visual e-commerce}: el usuario sube una foto y
        recibe los productos más similares.
  \item \textbf{Búsqueda musical}: por letra (full-text) y por similitud
        acústica (subiendo un audio o eligiendo una pista, con reproducción
        de las pistas del dataset desde la interfaz).
  \item \textbf{Consola de consultas multimodal}: un mini-lenguaje tipo SQL
        que unifica las búsquedas de las tres modalidades.
\end{enumerate}

% ══════════════════════════════════════════════════════════════
\section{Arquitectura del Sistema}

La Fig.~\ref{fig:pipeline} muestra el pipeline común. La
Tabla~\ref{tab:etapas} resume cómo se adapta cada etapa a cada modalidad: el
motor de búsqueda (\texttt{search\_sparse}) es idéntico para las tres.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  font=\scriptsize, node distance=0.35cm,
  stage/.style={draw, rounded corners=2pt, fill=blue!8,
                text centered, minimum width=4.6cm, minimum height=0.5cm},
  io/.style={draw, trapezium, trapezium left angle=70,
             trapezium right angle=110, fill=orange!15,
             text centered, minimum width=3.6cm, minimum height=0.5cm},
  arr/.style={-Stealth, thick}
]
\node[io] (in) {Contenido (texto / imagen / audio)};
\node[stage, below=of in]  (sp) {Split: chunks (párrafos / ventanas)};
\node[stage, below=of sp]  (ex) {Extractor: tokens / SIFT / MFCC};
\node[stage, below=of ex]  (cb) {Codebook: top-$k$ / K-Means ($k$ codewords)};
\node[stage, below=of cb]  (hi) {Histograma TF-IDF $L_2$-normalizado};
\node[stage, below=of hi]  (ix) {Índice invertido (SPIMI)};
\node[stage, below=of ix]  (se) {Búsqueda coseno (accumulator)};
\node[io, below=of se] (out) {Top-$N$ resultados con puntaje};

\draw[arr] (in)--(sp); \draw[arr] (sp)--(ex); \draw[arr] (ex)--(cb);
\draw[arr] (cb)--(hi); \draw[arr] (hi)--(ix); \draw[arr] (ix)--(se);
\draw[arr] (se)--(out);
\end{tikzpicture}
\caption{Pipeline unificado: solo el extractor y el codebook dependen de la
modalidad; el índice y la búsqueda se comparten.}
\label{fig:pipeline}
\end{figure}

\begin{table}[htbp]
\caption{Adaptación de cada etapa por modalidad}
\label{tab:etapas}
\centering
\scriptsize
\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Etapa} & \textbf{Texto} & \textbf{Imagen} & \textbf{Audio} \\
\midrule
Split      & estrofas & imagen del producto & ventanas de 3\,s \\
Extractor  & tokens (nltk) & SIFT 128-d & MFCC 40-d \\
Codebook   & top-$k$, $k{=}1024$ & K-Means, $k{=}512$ & K-Means, $k{=}128$ \\
Ponderación& \multicolumn{3}{c}{$\log(1{+}\mathrm{tf})\cdot\mathrm{idf}$, normalización $L_2$} \\
Índice     & \multicolumn{3}{c}{SPIMI $\rightarrow$ índice invertido (compartido)} \\
Búsqueda   & \multicolumn{3}{c}{coseno por acumuladores (compartida)} \\
\bottomrule
\end{tabular}
\end{table}

% ══════════════════════════════════════════════════════════════
\section{Estructura del Repositorio}

El código está organizado para que la arquitectura unificada se lea
directamente del árbol de carpetas: el \emph{motor} genérico vive en
\texttt{engine/} y no sabe nada de canciones ni de productos; las
aplicaciones lo orquestan desde \texttt{apps/} y lo exponen por HTTP en
\texttt{api/}.

\begin{lstlisting}
backend/
  app/
    engine/        motor generico (agnostico)
      split/         parrafos / ventanas de audio
      extractor/     sift, mfcc, color hsv
      codebook/      linguistico y k-means
      index/         spimi, indice invertido,
                     histogramas
      search/        busqueda coseno compartida
      query/         parser del mini-lenguaje SQL
    apps/          orquestadores por aplicacion
      music/         text_index, acoustic_index
      ecommerce/     visual_index
    api/           routers FastAPI: music,
                   ecommerce, compare, query
    comparisons/   propio vs GIN vs pgvector
                   (text, image, audio, ann)
    db/            sesion, repositorio y
                   adaptadores de persistencia
    core/          configuracion (k, rutas)
  pipelines/       ingest offline por modalidad
                   (+ --persist a Postgres)
  experiments/     benchmarks Fase 4 y results/
                   (CSV + graficos de este informe)
  tests/           suite pytest (motor y DB)
frontend/          UI React + Vite (3 pestanas)
db/init/           01_init.sql: schema + pgvector
                   + indices HNSW y GIN
docker-compose.yml db + backend + frontend
docs/              informe.tex y MANUAL.md
\end{lstlisting}

La separación tiene una regla simple: \texttt{engine/} solo conoce vectores e
histogramas (por eso el mismo índice y la misma búsqueda sirven para las tres
modalidades); \texttt{apps/} conoce los datasets y arma cada índice;
\texttt{api/} solo traduce HTTP a llamadas del motor; y \texttt{db/} es la
única capa que habla con PostgreSQL. Los artefactos de índice que produce el
\textit{ingest} (codebook, índice invertido y metadatos) se guardan en
\texttt{data/index/} y los datasets crudos en \texttt{data/raw/}; ninguno de
los dos se versiona en el repositorio.

% ══════════════════════════════════════════════════════════════
\section{Entregables}

\begin{itemize}
  \item \textbf{Repositorio GitHub} con el código fuente documentado y
        planificación en GitHub Projects.
  \item \textbf{Docker Compose} que levanta PostgreSQL con la extensión
        \texttt{pgvector}, el backend (FastAPI) y el frontend (React), con
        el esquema inicializado automáticamente desde
        \texttt{db/init/01\_init.sql}.
  \item \textbf{Tres aplicaciones funcionando} sobre el mismo motor:
        búsqueda visual e-commerce, búsqueda musical (letra + similitud
        acústica con reproducción de pistas) y consola de consultas
        multimodal.
  \item \textbf{Comparativas de la Fase 3}: GIN full-text y pgvector (exacto
        y HNSW), expuestas como endpoints \texttt{/compare/*} para
        demostrarlas en vivo.
  \item \textbf{Evaluación experimental de la Fase 4}: scripts de benchmark
        que generan los CSV y los gráficos incluidos en este informe
        (\texttt{backend/experiments/results/}).
  \item \textbf{Documentación}: este informe técnico y un manual de usuario
        (\texttt{docs/MANUAL.md}) con el uso de cada pestaña de la interfaz.
\end{itemize}

% ══════════════════════════════════════════════════════════════
\section{Instalación y Uso}

\subsection{Requisitos}
Docker (Compose incluido) y una cuenta de Kaggle para descargar los
datasets. No hace falta instalar Python ni Node en la máquina: todo corre en
contenedores.

\subsection{Levantar el sistema}
\begin{lstlisting}
docker compose up --build
# db:5432  backend:8000  frontend:5173
\end{lstlisting}

\subsection{Descargar los datasets}
Los datasets no están en el repositorio (van en \texttt{.gitignore} por
tamaño). Se descargan una vez de Kaggle y se dejan en \texttt{data/raw/}:

\begin{lstlisting}
# letras de canciones (texto)
kaggle datasets download \
  -d imuhammad/audio-features-and-lyrics-of-spotify-songs \
  -p data/raw/spotify --unzip

# productos de moda (imagen)
kaggle datasets download \
  -d paramaggarwal/fashion-product-images-small \
  -p data/raw/fashion --unzip

# pistas GTZAN (audio: csv de features + wavs)
kaggle datasets download \
  -d andradaolteanu/gtzan-dataset-music-genre-classification \
  -p data/raw/music --unzip
\end{lstlisting}

\subsection{Construir los índices (ingest)}
El \textit{ingest} corre una vez por modalidad: construye el codebook y el
índice invertido, guarda los artefactos en \texttt{data/index/} y, con
\texttt{--persist}, escribe todo en PostgreSQL:

\begin{lstlisting}
# texto (k=1024)
docker compose exec backend \
  python -m pipelines.ingest --app music \
  --data /data/raw/spotify/spotify_songs.csv \
  --k 1024 --persist

# imagen (k=512 + color)
docker compose exec backend \
  python -m pipelines.ingest --app ecommerce \
  --modality image \
  --data /data/raw/fashion/images \
  --k 512 --persist

# audio (k=128)
docker compose exec backend \
  python -m pipelines.ingest --app music \
  --modality audio \
  --data /data/raw/music/features_3_sec.csv \
  --k 128 --persist
\end{lstlisting}

\subsection{Usar las aplicaciones}
\begin{itemize}
  \item \textbf{Interfaz gráfica}: \url{http://localhost:5173}, con una
        pestaña por aplicación.
  \item \textbf{API (Swagger)}: \url{http://localhost:8000/docs}.
  \item \textbf{Comparativas en vivo}: \texttt{GET /compare/text?q=...},
        \texttt{GET /compare/vector?external\_id=...} y
        \texttt{GET /compare/audio?filename=...} devuelven los resultados y
        la latencia de cada técnica para la misma consulta.
\end{itemize}

\subsection{Reproducir los experimentos}
\begin{lstlisting}
# comparativa de texto (4 tamanos de corpus)
docker compose exec backend \
  python -m experiments.benchmark \
  --data /data/raw/spotify/spotify_songs.csv \
  --sizes 1000 5000 10000 18454

# HNSW vs busqueda exacta
docker compose exec backend \
  python -m experiments.ann_benchmark
\end{lstlisting}
Ambos scripts regeneran los CSV y los gráficos de
\texttt{backend/experiments/results/} usados en este informe. La suite de
tests se corre con \texttt{pytest} desde \texttt{backend/} (los tests de
Postgres se saltan si no hay base disponible).

% ══════════════════════════════════════════════════════════════
\section{Datasets}

\begin{itemize}
  \item \textbf{Texto:} \textit{Audio features and lyrics of Spotify songs}
        (Kaggle): 18\,454 canciones con letra, multilingüe.
  \item \textbf{Imagen:} \textit{Fashion Product Images (Small)} (Kaggle):
        44\,326 fotos de productos de moda ($\sim$965\,000 descriptores SIFT
        en total).
  \item \textbf{Audio:} \textit{GTZAN} (Kaggle): 1\,000 pistas de 30\,s en
        10 géneros; se usan las ventanas de 3\,s con 20 MFCC (media +
        varianza $=40$ características por ventana) y los \texttt{.wav} para
        la reproducción desde la interfaz.
\end{itemize}

El \textit{ingest} acepta \texttt{.csv}, \texttt{.json} y \texttt{.parquet}
y mapea columnas por alias, por lo que los datasets son intercambiables.

% ══════════════════════════════════════════════════════════════
\section{Implementación por Módulo}

\subsection{Extractores y codebook}
\textbf{Texto}: tokenización $\rightarrow$ minúsculas $\rightarrow$
\textit{stopwords} (ES+EN) $\rightarrow$ \textit{stemming}
(\texttt{SnowballStemmer}); el codebook son las $k$ raíces más frecuentes de
la colección. \textbf{Imagen}: descriptores locales SIFT y, como
complemento, un histograma de color HSV de 32 bins (SIFT trabaja en escala de
grises y en moda el color discrimina). \textbf{Audio}: MFCC por ventana
deslizante. Para imagen y audio, el codebook se obtiene con
\texttt{MiniBatchKMeans} sobre todos los descriptores de la colección: los
$k$ centroides son las \textit{visual words} / \textit{acoustic words}.

\subsection{Cuantización TF-IDF}
Cada chunk se representa como un histograma de codewords ponderado con TF
sublineal e IDF, y normalizado en $L_2$:
\begin{equation}
  w_{i} = \log\!\big(1+\mathrm{tf}_{i}\big)\cdot
          \log_{10}\!\frac{N}{\mathrm{df}_{i}},
\end{equation}
donde $N$ es el número de ítems y $\mathrm{df}_i$ en cuántos aparece la
codeword $i$. El TF sublineal evita que una codeword muy repetida (el coro de
una canción, una textura repetitiva) domine el histograma; el IDF penaliza
las poco discriminativas. Con la normalización $L_2$, el coseno entre dos
histogramas se reduce a un producto punto.

El tamaño $k$ del codebook es la perilla central del \textit{trade-off}
calidad/costo: con $k$ chico la cuantización confunde contenidos distintos;
con $k$ grande discrimina más, a costa de \textit{posting lists} más largas.
Usamos $k{=}1024$ (texto), $k{=}512{+}32$ (imagen con color) y $k{=}128$
(audio), respetando el tope de 2\,000 dimensiones del HNSW de pgvector.

En imagen, el histograma final concatena la parte visual y la de color
manteniendo la norma unitaria:
$h = [\sqrt{1-\alpha}\,\mathrm{BoVW} \,\Vert\, \sqrt{\alpha}\,\mathrm{color}]$,
con lo que el coseno queda como promedio ponderado por $\alpha$ entre
similitud de forma y de color.

\subsection{Índice invertido con SPIMI}
El índice invertido mapea cada codeword a sus \textit{postings}
$(\mathit{chunk}, \mathit{peso})$. Se construye con SPIMI
(Algoritmo~\ref{alg:spimi}), que escala a colecciones que no caben en RAM.

\begin{algorithm}[htbp]
\caption{SPIMI: construcción del índice invertido}
\label{alg:spimi}
\begin{algorithmic}[1]
\STATE \textbf{Entrada:} stream de $(\mathit{chunk}, \mathit{hist\ disperso})$
\STATE \textbf{Salida:} índice invertido en disco
\STATE $B \gets \{\}$ \COMMENT{bloque en memoria}
\FORALL{$(c, h)$ en el stream}
  \FORALL{$(w, \mathit{peso})$ en $h$}
    \STATE agregar $(c, \mathit{peso})$ a $B[w]$
  \ENDFOR
  \IF{$|B| \ge \mathit{block\_size}$}
    \STATE volcar $B$ a disco \emph{ordenado por} $w$; $B \gets \{\}$
  \ENDIF
\ENDFOR
\STATE volcar el bloque final
\RETURN \textit{k-way merge} de los bloques (heap por $w$)
\end{algorithmic}
\end{algorithm}

\subsection{Búsqueda por coseno (accumulator)}
La búsqueda (Algoritmo~\ref{alg:cosine}) solo visita los chunks que comparten
alguna codeword con la consulta, que es lo que hace eficiente al índice
invertido frente a un escaneo denso. Es idéntica para las tres modalidades.

\begin{algorithm}[htbp]
\caption{CosineScore sobre el índice invertido}
\label{alg:cosine}
\begin{algorithmic}[1]
\STATE \textbf{Entrada:} histograma disperso $q$ de la consulta, top-$N$
\STATE $S \gets$ mapa de acumuladores en 0
\FORALL{$(w, q_w)$ en $q$}
  \FORALL{$(c, d_w)$ en $\mathit{postings}(w)$}
    \STATE $S[c] \gets S[c] + q_w \cdot d_w$
  \ENDFOR
\ENDFOR
\FORALL{$c$ en $S$}
  \STATE $S[c] \gets S[c] \,/\, (\lVert q\rVert \cdot \lVert d_c\rVert)$
\ENDFOR
\RETURN los $N$ chunks de mayor $S[c]$
\end{algorithmic}
\end{algorithm}

\subsection{Persistencia en PostgreSQL}
El esquema (\texttt{db/init/01\_init.sql}) guarda \texttt{items},
\texttt{chunks}, \texttt{codebook} (centroides como \texttt{vector} de
pgvector), \texttt{histograms} (histograma por chunk) e
\texttt{inverted\_index} (postings). Se inicializa automáticamente con Docker
Compose.

% ══════════════════════════════════════════════════════════════
\section{Comparativas Nativas en PostgreSQL}

\subsection{Texto: GIN full-text}
Cada chunk lleva un \texttt{tsvector} indexado con GIN; la consulta nativa
usa \texttt{plainto\_tsquery} y rankea con \texttt{ts\_rank}:
\begin{lstlisting}[language=SQL]
SELECT i.id, max(ts_rank(c.tsv, q)) AS rank
FROM chunks c JOIN items i ON i.id=c.item_id,
     plainto_tsquery('spanish', %s) AS q
WHERE c.modality='text' AND c.tsv @@ q
GROUP BY i.id ORDER BY rank DESC LIMIT %s;
\end{lstlisting}

\subsection{Vectores: pgvector con HNSW}
Los histogramas se consultan por distancia coseno (\texttt{<=>}) y se indexan
con HNSW, un índice parcial por modalidad cuya dimensión coincide con la $k$
del codebook (texto 1024, imagen 544, audio 128):
\begin{lstlisting}[language=SQL]
CREATE INDEX ON histograms
  USING hnsw ((hist::vector(1024))
              vector_cosine_ops)
  WITH (m=16, ef_construction=64)
  WHERE modality='text';
\end{lstlisting}
Sin el índice, pgvector ejecuta un \textit{Seq Scan} (búsqueda exacta); con
él, búsqueda aproximada cuya voracidad controla \texttt{hnsw.ef\_search}.

% ══════════════════════════════════════════════════════════════
\section{Evaluación Experimental}

\subsection{Texto: propio vs.\ GIN vs.\ pgvector}
Se corrió la misma batería de 30 consultas sobre corpus de 1K, 5K, 10K y
18\,454 chunks (Tabla~\ref{tab:texto}); el recall@10 toma como referencia el
ranking coseno del índice propio. Las Figs.~\ref{fig:latencia} y
\ref{fig:memoria} muestran latencia y memoria según el tamaño.

\begin{table}[htbp]
\caption{Comparativa de texto por tamaño de corpus ($k{=}1024$)}
\label{tab:texto}
\centering
\scriptsize
\begin{tabular}{@{}rlrrrr@{}}
\toprule
\textbf{Corpus} & \textbf{Método} & \textbf{Media} & \textbf{p95} &
\textbf{QPS} & \textbf{R@10} \\
 & & (ms) & (ms) & & \\
\midrule
1\,000  & índice invertido & 0.44  & 0.71  & 2284 & 1.00 \\
        & GIN full-text    & 1.45  & 1.97  & 688  & 0.07 \\
        & pgvector coseno  & 10.35 & 12.00 & 97   & 1.00 \\
\midrule
5\,000  & índice invertido & 1.21  & 2.20  & 824  & 1.00 \\
        & GIN full-text    & 1.23  & 1.73  & 810  & 0.08 \\
        & pgvector coseno  & 22.99 & 24.47 & 44   & 1.00 \\
\midrule
10\,000 & índice invertido & 2.14  & 3.64  & 466  & 1.00 \\
        & GIN full-text    & 2.51  & 4.63  & 399  & 0.05 \\
        & pgvector coseno  & 25.84 & 29.91 & 39   & 1.00 \\
\midrule
18\,454 & índice invertido & 4.49  & 7.36  & 223  & 1.00 \\
        & GIN full-text    & 3.67  & 9.54  & 273  & 0.04 \\
        & pgvector coseno  & 76.08 & 88.75 & 13   & 1.00 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{latency_vs_size.png}
\caption{Latencia media según el tamaño del corpus (texto).}
\label{fig:latencia}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{memory_vs_size.png}
\caption{Memoria en disco de los índices nativos.}
\label{fig:memoria}
\end{figure}

\textbf{Lectura.} El índice propio domina la latencia hasta $\sim$10K chunks;
en el corpus completo GIN lo empata (3.7 vs.\ 4.5\,ms) porque el vocabulario
de $k{=}1024$ engorda las posting lists propias (889\,366 postings).
\texttt{pgvector} recupera \emph{exactamente} lo mismo que el motor propio
(recall $=1.0$ en todos los tamaños: ambos rankean por coseno sobre el mismo
histograma TF-IDF), pero paga la distancia densa de 1\,024 dimensiones
(76\,ms y $\sim$400\,MB de histogramas frente a $\sim$27\,MB del GIN). El
recall de GIN frente al coseno es bajo y cae al crecer el corpus
($0.07 \rightarrow 0.04$): la coincidencia booleana de tokens responde una
pregunta distinta a la del ranking por similitud.

\subsection{ANN: HNSW vs.\ búsqueda exacta}
Ambas vías ejecutan la misma consulta coseno; solo cambia si el planner usa
el índice HNSW o cae a Seq Scan. El recall del aproximado se mide contra el
exacto (Tabla~\ref{tab:ann}, Fig.~\ref{fig:ann}).

\begin{table}[htbp]
\caption{HNSW vs.\ exacto (histogramas de texto, 1\,024 dims)}
\label{tab:ann}
\centering
\scriptsize
\begin{tabular}{@{}lrrrr@{}}
\toprule
\textbf{Método} & \texttt{ef\_search} & \textbf{Media (ms)} & \textbf{QPS} &
\textbf{R@10} \\
\midrule
exacto (Seq Scan) & ---  & 47.82 & 21  & 1.00 \\
HNSW              & 10   & 1.49  & 670 & 0.74 \\
HNSW              & 20   & 1.48  & 676 & 0.85 \\
HNSW              & 40   & 2.04  & 491 & 0.87 \\
HNSW              & 80   & 2.72  & 368 & 0.93 \\
HNSW              & 160  & 4.02  & 249 & 0.96 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{ann_recall_vs_latency.png}
\caption{HNSW: curva recall--latencia al variar \texttt{ef\_search}.}
\label{fig:ann}
\end{figure}

\textbf{Lectura.} HNSW es 12--32$\times$ más rápido que el escaneo exacto
(47.8\,ms $\rightarrow$ 1.5--4\,ms) a costa de recall, y
\texttt{ef\_search} controla el \textit{trade-off}: con 160 se alcanza
recall $0.96$ a $\sim$250 QPS. Con histogramas TF-IDF de 1\,024 dimensiones el
grafo navega mejor que con vocabularios chicos: incluso \texttt{ef\_search}=10
rinde recall $0.74$.

\subsection{Audio: propio vs.\ pgvector}
Sobre las 1\,000 pistas de GTZAN, ambas vías devuelven \emph{exactamente el
mismo ranking con los mismos puntajes} (coseno sobre los mismos histogramas
de acoustic words). Ejemplo con \texttt{blues.00000.wav}:
top-3 $=$ \{\texttt{rock.00071} (0.7321), \texttt{blues.00059} (0.6058),
\texttt{blues.00080} (0.5999)\}. La latencia fue $\sim$0.4\,ms (propio)
frente a $\sim$10--12\,ms (pgvector): con histogramas dispersos, el índice
invertido compacto responde lo mismo a una fracción del costo.

% ══════════════════════════════════════════════════════════════
\section{Análisis de Trade-offs y Conclusiones}

\begin{itemize}
  \item \textbf{¿Qué técnica ganó en qué métrica?} En latencia, el índice
        propio gana hasta corpus medianos y GIN lo empata en el corpus
        completo; en memoria gana el propio (solo postings). En búsqueda
        vectorial, HNSW domina la latencia (12--32$\times$) al precio de
        recall.
  \item \textbf{¿Se recupera la misma información?} El índice propio y
        pgvector coinciden exactamente (recall $=1.0$; en audio con puntajes
        idénticos) porque comparten la matemática. GIN responde una semántica
        booleana distinta (recall 0.04--0.08 frente al coseno).
  \item \textbf{Exactitud vs.\ velocidad.} La cuantización y el ANN
        introducen falsos positivos/negativos; a cambio el sistema es rápido
        y compacto. La calidad depende de $k$ y de \texttt{ef\_search}.
  \item \textbf{Memoria vs.\ latencia.} pgvector es cómodo (SQL puro) y
        devuelve lo mismo que el motor propio, pero con $k{=}1024$ pesa
        $\sim$400\,MB y el escaneo denso llega a 76\,ms; GIN se mantiene en
        $\sim$27\,MB.
\end{itemize}

\textbf{Limitaciones.} La cuantización pierde detalle fino de los
descriptores; las dimensiones de los índices HNSW quedan acopladas a la $k$
del codebook (cambiar una implica reindexar); y el histograma de texto
degenera si el vocabulario es menor que $k$.

\textbf{Recomendación.} Usar el índice invertido propio como motor primario
(rápido y barato); pgvector+HNSW cuando se quiera delegar la búsqueda
vectorial a la base de datos; y reservar GIN para búsqueda literal por
palabras clave, no para similitud.

\end{document}
